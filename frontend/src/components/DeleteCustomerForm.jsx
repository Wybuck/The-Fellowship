const DeletePersonForm = ({ rowObject, backendURL, refreshcustomers }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeletePersonForm;